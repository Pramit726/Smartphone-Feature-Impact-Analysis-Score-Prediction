apiVersion: v1
kind: Service
metadata:
  name: smartphone-service
spec:
  type: LoadBalancer
  selector:
    app: smartphone-api
  ports:
  - protocol: TCP
    port: 80        # Port accessible from outside
    targetPort: 8000 # Port FastAPI is listening on inside the container